\usepackage[
    nott,      % do not load not monospace
    sf,        % only load sans-serif
    sfdefault, % activate sans-serif
    % numerals
    lining,
    tabular,
    % light font
    light,
    semibold,
]{noto}
